---
import ProductGrid from './ProductGrid.astro';
import type { ProductWithSku } from '../lib/catalog';

export interface Props {
  headingId: string;
  title: string;
  description: string;
  items: ProductWithSku[];
  className?: string;
  /** Plan 215: id de sección para la barra de anclas (navegación por chips). */
  sectionId?: string;
  gridId?: string;
  gridClassName?: string;
  showProfileStrip?: boolean;
  compactMobile?: boolean;
}

const {
  headingId,
  title,
  description,
  items,
  className = '',
  sectionId,
  gridId,
  gridClassName = 'home-collection-grid',
  showProfileStrip = false,
  compactMobile = true,
} = Astro.props;
---

<section class={`home-section ${className}`.trim()} aria-labelledby={headingId} id={sectionId}>
  <div class="home-section__header">
    <h2 id={headingId}>{title}</h2>
    <p data-home-personalized-note={showProfileStrip ? 'true' : undefined}>{description}</p>
  </div>

  {
    showProfileStrip ? (
      <div class="home-profile-strip" data-home-profile-strip aria-live="polite">
        <div data-home-profile-copy>
          <strong>Tu último pedido puede quedar listo en un toque.</strong>
          <span>Si quieres, deja una nota para el pedido y úsala también en la próxima compra.</span>
        </div>
        <button
          type="button"
          class="btn btn-outline-dark home-profile-strip__repeat"
          data-repeat-last-order
          disabled
          aria-disabled="true"
        >
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><polyline points="1 4 1 10 7 10"/><path d="M3.51 15a9 9 0 1 0 2.13-9.36L1 10"/></svg>
          Repetir último pedido
        </button>
      </div>
    ) : null
  }

  <ProductGrid
    items={items}
    id={gridId}
    className={gridClassName}
    compactMobile={compactMobile}
  />
</section>
