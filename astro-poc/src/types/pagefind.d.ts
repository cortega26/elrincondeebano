// Type declarations for pagefind (generated at build time in dist/pagefind/)
declare module '/pagefind/pagefind.js' {
  export class PagefindUI {
    constructor(options: {
      element: string;
      showSubResults?: boolean;
      showImages?: boolean;
      excerptLength?: number;
      autofocus?: boolean;
    });
  }
}
