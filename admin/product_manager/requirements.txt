# Top-level dependencies for admin tooling.
# Reproducible versions are enforced via requirements.lock.txt in CI:
#   pip install -r requirements.txt -c requirements.lock.txt
pytest
pytest-mock
pylint
ruff
portalocker
mypy
pytest-cov
pytest-xdist
bandit
pip-audit
types-Pillow
