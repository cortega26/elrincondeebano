# Top-level dependencies for admin tooling.
# Reproducible versions are enforced via requirements.lock.txt in CI:
#   pip install -r requirements.txt -c requirements.lock.txt
pytest
pytest-mock
pylint>=4.0.4,<4.1
# Keep astroid aligned with pylint 4.0.x to avoid resolver conflicts in CI.
astroid>=4.0.2,<4.2
ruff
portalocker
mypy
pytest-cov
pytest-xdist
bandit
pip-audit
types-Pillow
