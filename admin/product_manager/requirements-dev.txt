# Development, testing, linting, and security-audit tooling.
# Install together with the runtime requirements and the version lock:
#   pip install -r requirements.txt -r requirements-dev.txt -c requirements.lock.txt
pytest
pytest-mock
pylint>=4.0.6,<4.1
# Keep astroid aligned with pylint 4.0.x to avoid resolver conflicts in CI.
astroid>=4.0.2,<4.1
ruff
mypy
pytest-cov
pytest-xdist
bandit
pip-audit
types-Pillow
