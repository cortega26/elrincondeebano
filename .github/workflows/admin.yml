name: Admin Tools CI

on:
  push:
    paths:
      - 'admin/**'
      - 'tools/category_og/**'
      - 'config/category_og_icon_map.json'
      - 'assets/images/og/icons/**'
      - 'assets/images/og/categories/**'
  pull_request:
    paths:
      - 'admin/**'
      - 'tools/category_og/**'
      - 'config/category_og_icon_map.json'
      - 'assets/images/og/icons/**'
      - 'assets/images/og/categories/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test-ts:
    name: TypeScript Content Manager
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    defaults:
      run:
        working-directory: admin/content-manager
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: ./.github/actions/setup-node-and-deps

      - name: Lint
        # Plan 127 F1.2: the FULL tree (src + scripts + test) must be
        # error-free in CI — previously only lint-staged covered the admin.
        run: npx eslint src scripts test --config eslint.config.mjs

      - name: Typecheck
        run: npx tsc -p tsconfig.json --noEmit

      - name: Unit and integration tests
        run: npx vitest run

      - name: Test coverage
        run: npx vitest run --coverage

      - name: Dependency audit
        run: npm audit --omit=dev

      - name: Build web app
        run: npm run build

      - uses: ./.github/actions/setup-playwright

      - name: E2E smoke tests
        run: npx playwright test -c playwright.config.ts

      - name: E2E import workflow (isolated temp repo)
        run: npx playwright test -c playwright.import.config.ts

      - name: E2E change-set control center (isolated temp repo)
        run: npx playwright test -c playwright.changes.config.ts

      - name: E2E media workbench (isolated temp repo)
        run: npx playwright test -c playwright.media.config.ts

      - name: E2E storefront curation (isolated temp repo)
        run: npx playwright test -c playwright.storefront.config.ts

      - name: E2E pagination/bulk/reorder scope (isolated temp repo)
        run: npx playwright test -c playwright.scope.config.ts

      - name: Certification report
        run: node --import tsx scripts/certification-report.ts --ci

      - name: Rollback and failure drills
        run: node --import tsx scripts/rollback-drill.ts

      - name: Upload E2E artifacts on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: admin-ts-e2e-artifacts
          path: |
            admin/content-manager/test-results
            admin/content-manager/playwright-report
            reports/e2e
            reports/certification
          if-no-files-found: ignore
